spring:
  datasource:
    url: jdbc:mysql://localhost:3306/testedb
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # ou create-drop / validate / none / create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

